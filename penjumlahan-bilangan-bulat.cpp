#include <iostream>
using namespace std;

int main() {
    int a, b, c, d, t;
    
    cout << "Masukan nilai A: ";
    cin >> a;
    cout << "Masukan nilai B: ";
    cin >> b;
    cout << "Masukan nilai C: ";
    cin >> c;
    cout << "Masukan nilai D: ";
    cin >> d;

    t = a + b + c + d;

    cout << "Output: " << t << endl;

    return 0;
}
